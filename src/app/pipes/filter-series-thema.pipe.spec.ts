import { FilterSeriesThemaPipe } from './filter-series-thema.pipe';

describe('FilterSeriesThemaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSeriesThemaPipe();
    expect(pipe).toBeTruthy();
  });
});
