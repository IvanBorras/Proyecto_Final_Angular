import { FilterMoviesThemaPipe } from './filter-movies-thema.pipe';

describe('FilterMoviesThemaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMoviesThemaPipe();
    expect(pipe).toBeTruthy();
  });
});
